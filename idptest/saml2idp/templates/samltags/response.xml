{% load samltags %}
<samlp:Response xmlns:samlp="urn:oasis:names:tc:SAML:2.0:protocol"
                xmlns:saml="urn:oasis:names:tc:SAML:2.0:assertion"
                ID="{{ saml_response.id }}"
                Version="2.0"
                IssueInstant="{{ saml_response.issue_instant }}"
                Destination="{{ saml_request.acs_url }}"
                InResponseTo="{{ saml_request.id }}"
                >
{% if issuer %}    <saml:Issuer>{{ issuer }}</saml:Issuer>{% endif %}
{% if signature %}{% indent 4 %}{% signature_xml signature %}{% endindent %}{% endif %}
    <samlp:Status>
        <samlp:StatusCode Value="urn:oasis:names:tc:SAML:2.0:status:Success"></samlp:StatusCode>
    </samlp:Status>
{% indent 4 %}{% assertion_xml saml_request assertion issuer %}{% endindent %}
</samlp:Response>
