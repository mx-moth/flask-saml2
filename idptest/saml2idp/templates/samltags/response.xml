{% load samltags %}<samlp:Response xmlns:samlp="urn:oasis:names:tc:SAML:2.0:protocol" ID="{{ saml_response.id }}" Version="2.0" IssueInstant="{{ saml_response.issue_instant }}" Destination="{{ saml_request.acs_url }}" InResponseTo="#{{ saml_request.id }}">{% if issuer %}<saml:Issuer xmlns:saml="urn:oasis:names:tc:SAML:2.0:assertion">{{ issuer }}</saml:Issuer>{% endif %}{% if signature_xml %}{{ signature_xml|safe }}{% endif %}<samlp:Status><samlp:StatusCode Value="urn:oasis:names:tc:SAML:2.0:status:Success"></samlp:StatusCode></samlp:Status>{{ assertion_xml|safe }}</samlp:Response>
